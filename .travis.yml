sudo: required

language: go

go:
  - 1.9.x
  
services:
  - docker

addons:
  apt:
    packages:
      - docker-ce

install:
  - echo "Please don't go get"

script:
  - cd queue-worker && ./build.sh
